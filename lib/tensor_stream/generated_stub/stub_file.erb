# This file has ben automatically generated by stubgen
# DO NOT EDIT
#
module TensorStream
  module OpStub
<% TensorStream::OpMaker.each_op do |op|%>
    ##
    # <%= op.description %>
    #
    <% if op.supports_broadcasting? %># This operation supports broadcasting<% end %>
    # Params:
    #<% op.parameters.each do |param| %>
    # +<%= param[:name] %>+:: <%= param[:description]%><% end %>
    def <%= op.operation.to_s %>(<%= (op.expand_params(true) + op.expand_options(true)).join(', ') %>)
      <% if op.check_types? %><%= op.expand_params(false).join(', ') %> = check_data_types(<%= op.expand_params(false).join(', ') %>)<% end %>
      _op(:<%= op.operation %>, <%= (op.expand_params(false) + op.options_call).join(', ') %>)
    end<% end %>
  end
end